import 'package:flutter/material.dart';

class OrganiserDasboard extends StatelessWidget {
  const OrganiserDasboard({super.key});
  
  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Organiser Dashboard', style: TextStyle(fontSize: 24)),
    );
  }
}